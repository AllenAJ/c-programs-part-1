#include <stdio.h>
#include <conio.h>

void main()
{
    float a,b,prod;
    printf("Enter two numbers : ");
    scanf("%f %f",&a,&b);
    prod=a*b;
    printf("product =%f",prod);
    getch();
}
